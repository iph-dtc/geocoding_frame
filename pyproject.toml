[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
dependencies = [
    'geocoding_frame @ git+https://github.com/iph-dtc/geocoding_frame.git',
]
